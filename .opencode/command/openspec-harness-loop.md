---
description: Recommend the next legal OpenSpec Harness loop action
argument-hint: "[change]"
---

Load and follow the skill `openspec-harness-loop` for: $ARGUMENTS

Run the internal loop recommender as the source of truth. Report the next legal
action, blocked gates, and exact user-facing command to run next. Do not invent
blockers beyond the loop or verifier result; `design.md` is optional and
`evidence.md` is an archive gate.
