---
description: Create OpenSpec proposal artifacts for a change
argument-hint: "<change>"
---

Load and follow the skill `openspec-harness-propose` for: $ARGUMENTS

Create or update `proposal.md`, `design.md` when needed, `test.md`, `tasks.md`,
and delta specs. Define task-level Test lines and the change-level test plan
inside proposal work. Do not ask the user to run a separate testing workflow.
Do not start implementation until business, optional design, and test review
gates are satisfied.
