---
description: Check docs against plugin commands, skills, tools, and package metadata
---

Load and follow the skill `openspec-harness-docs-sync` for: $ARGUMENTS

First call the internal `openspec_harness_docs_sync` tool when available and use
its result as the baseline. Check README, design docs, context docs, opencode
commands, skills, plugin tools, and package metadata for drift. Report or repair
documentation drift only; do not implement product changes unless explicitly
requested.
